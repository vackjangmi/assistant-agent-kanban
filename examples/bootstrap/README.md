# Example Bootstrap

Use `./init.sh` or `assistant_agent_kanban.config.load_config(...).bootstrap()` to initialize a kanban root.

The application creates the required state and runtime directories automatically, including state columns, locks, workspaces, runs, request drafts, request uploads, board cache, and retrospectives.
